import 'package:flutter/material.dart';

class favouriteScreen extends StatelessWidget {
  const favouriteScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("I am in favourite screen"),
      ),
    );
  }
}
